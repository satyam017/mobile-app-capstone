# Mindful — Mobile App Capstone

React Native (Expo) app for the Mobile App Development Capstone.

## Run

```bash
cd mobile-app
npm start
```

Then press `i` for iOS simulator, `a` for Android emulator, or scan the QR code with Expo Go.
